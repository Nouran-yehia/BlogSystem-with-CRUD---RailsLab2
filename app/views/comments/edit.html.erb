<%= form_for([@post, @comment]) do |f| %>
    
    <div class="field">
      <div class="control">
        <%= f.text_field :content ,value:@comment.content, class:"textarea" %>
        </div>
    
        </div>
    </div>
    <%= submit_tag :'Edit Comment', class:"btn btn-primary"%>
    <% end %>