<%= form_for([@post, @comment]) do |f| %>
    
    <div class="field">
      <div class="control">
        <%= f.text_field :content ,placeholder:"Write your comment here...",class:"textarea" %>
      </div>
    
        </div>
    </div>
    <%= submit_tag :'Add Comment', class:"btn btn-primary"%>
    <% end %>